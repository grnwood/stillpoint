Install to OS.

$ sudo ./install-app.sh

Quick Capture:
- Run `stillpoint --quick-capture` to open the capture overlay.
- You can bind this command to a custom keyboard shortcut in your desktop environment.
