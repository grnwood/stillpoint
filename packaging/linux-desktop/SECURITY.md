Unsigned Build Notice

Linux generally does not show OS-level warnings for unsigned binaries, but
you can still verify download integrity with a SHA256 hash.

Optional integrity check:
- Compute a SHA256 hash and compare it to the published release hash (posted at
  `https://github.com/grnwood/StillPoint/releases`).
  Command:
  `sha256sum <artifact>`
